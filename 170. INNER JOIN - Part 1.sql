# Guna query ni untuk check values dari semua field dalam table dept_manager_dup:

SELECT 
    *
FROM
    employees.dept_manager_dup
ORDER BY dept_no ASC;

# Guna query ni untuk check values dari semua field dalam table departments__dup:

SELECT 
    *
FROM
    employees.departments_dup
ORDER BY dept_no ASC;
